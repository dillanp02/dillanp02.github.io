import styled from "styled-components";
import { DEFAULT_PAGE_MARGIN } from "../../constants/css-constants";

export const AppContainer = styled.div`
	position: relative;
	min-height: 100vh;
	padding-bottom: ${DEFAULT_PAGE_MARGIN};
`;
